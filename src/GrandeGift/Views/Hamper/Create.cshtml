@model CreateHamperViewModel

<div class="page-banner manage-hamper-banner"></div>
<h1 class="page-title">Create Hamper</h1>

<div class="container userForm">
    <form asp-action="Create" asp-controller="Hamper" method="post">
        <div class="form-group">
            <label>Name</label>
            <input type="text" asp-for="@Model.Name" class="form-control" />
        </div>
        <div class="form-group">
            <label>Description</label>
            <input type="text" asp-for="@Model.Description" class="form-control" />
        </div>
        <div class="form-group">
            <label>Price</label>
            <input type="text" asp-for="@Model.Price" class="form-control" />
        </div>
        <div class="form-group">
            <label>Category</label>
            <select class="form-control" asp-for="@Model.SelectedCategoryId">
                @foreach (var item in Model.Categories)
                {
                    <option value="@item.CategoryId">@item.Name</option>
                }
            </select>
            <a asp-action="Create" asp-controller="Category">Create Category</a>
        </div>
        <div class="form-group">
            <label>Picture</label>
            <input type="file" class="form-control" />
        </div>

        <a>Add gifts to this Hamper</a>

        <button type="submit" class="btn btn-success">Create</button>
    </form>
</div>